# Event Review Data

This folder is for local administrator review data generated by `tools/sync-events.js`.

The JSON queues are intentionally ignored by Git:

- `pending-events.json`
- `approved-events.json`
- `rejected-events.json`

Only approved candidates are published to KIS by creating `cards/event_*.md` files.

